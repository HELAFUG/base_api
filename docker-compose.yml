services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: qwerty
      POSTGRES_DB: base_api_db

    ports:
      - 5431:5432

    volumes:
      - pgdata:/var/lib/postgresql


volumes:
  pgdata: